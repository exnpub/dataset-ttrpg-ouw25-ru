# План подготовки корпуса для ML

## Цель

Преобразовать исходные документы из `docx_sources/` в проверяемый и
воспроизводимый корпус Markdown, который удобно читать, искать, разбивать на
фрагменты и использовать в RAG, дообучении и иных ML-задачах. Исходные DOCX
остаются неизменяемым первоисточником.

На момент составления плана в `docx_sources/` 39 документов (около 61 МБ), из
них 32 содержат встроенные медиа. Поэтому текст и бинарные вложения должны
экспортироваться и проверяться совместно.

## Текущий статус — 2026-07-27

**Версия 1.0.0: Корпус готов.** Версия 1.1.0: Публикация на Hugging Face.

### Версия 1.0.0 (ЗАВЕРШЕНО)

**Корпус полностью подготовлен к использованию в локальных ML-проектах.**

Выполнена первая воспроизводимая сборка корпуса:

* 39 DOCX сконвертированы с помощью Pandoc 3.10.1 в `content/imported/` и
  нормализованы в `content/markdown/`;
* медиа извлечены в `content/assets/`, а ссылки в Markdown сделаны
  относительными;
* добавлены идентификаторы, хеши источников и YAML-метаданные;
* draft-ссылки вида `<URL>` и `<[подпись](URL)>` нормализованы до обычного
  Markdown-вида `[подпись](URL)`;
* сформированы `documents.jsonl` и 1 362 смысловых фрагмента в
  `chunks.jsonl`;
* добавлены скрипты сборки, структурного QA-аудита и проверки в `scripts/`;
* структурный QA-аудит сверил все 39 DOCX с Markdown без расхождений по
  вложенным изображениям, таблицам и draft-ссылкам;
* в Markdown и обоих JSONL-индексах зафиксированы `license: "Public Domain"`
  и `license_scope: "text"`.

**Все 39 документов проверены и отмечены статусом `reviewed`.** Лицензирование материалов как
`Public Domain` подтверждено правообладателем. Корпус готов к публикации и использованию
в ML-проектах. Все открытые пункты закрыты и зарегистрированы в
`content/manifests/issues.json` с статусом `closed`.

## Результат и принципы

* Markdown — канонический текстовый слой; DOCX не редактируются и не удаляются.
* Каждому исходнику соответствует один Markdown-документ с устойчивым `id` и
  сохранённой связью с оригиналом.
* Иллюстрации, таблицы, ссылки, сноски и шаблоны не теряются молча: они либо
  корректно конвертированы, либо явно отмечены для ручной обработки.
* Смысловая правка отделена от технической конвертации. Это позволяет
  воспроизводить экспорт и видеть происхождение каждого изменения.
* Производные файлы для поиска и ML строятся из нормализованного Markdown, а не
  редактируются вручную.

## Целевая структура репозитория

```text
docx_sources/                 # неизменяемые первоисточники
content/
  markdown/                   # нормализованные документы (.md)
  assets/                     # извлечённые изображения, по папкам документов
  manifests/                  # метаданные документов и журнал проверки
  chunks/                     # производные фрагменты для индексации
  indexes/                    # JSONL/CSV-каталоги, создаваемые скриптом
scripts/                      # конвертация, нормализация, валидация, сборка
tests/                        # регрессионные проверки разметки (по необходимости)
docs/
  CONTRIBUTING.md             # правила ручной разметки и именования
```

Создавать эти каталоги следует вместе с реализацией соответствующего этапа,
чтобы не появлялись пустые или неиспользуемые артефакты.

## Этапы

### 1. Инвентаризация и правила корпуса [DONE]

1. [x] Сформировать машиночитаемый реестр всех DOCX...
2. [x] Определить лицензию...
3. [x] Согласовать правила преобразования...
4. [x] Ввести статусы...

**Критерий готовности:** реестр покрывает 39 исходников и позволяет обнаружить
дубликат, переименование или изменение DOCX. [ВЫПОЛНЕНО]

### 2. Воспроизводимая первичная конвертация [DONE]

1. [x] Выбрать и зафиксировать конвертер...
2. [x] Написать скрипт пакетной конвертации...
3. [x] Создавать безопасные латинские slug-имена файлов...
4. [x] Не смешивать автоэкспорт и ручные исправления...

**Критерий готовности:** скрипт повторно запускается без ручных действий;
каждому DOCX сопоставлен Markdown или зафиксирована конкретная ошибка. [ВЫПОЛНЕНО]

### 3. Нормализация Markdown и ручная вычитка [IN PROGRESS]

Для каждого документа привести материал к единому виду:

* [x] один H1, далее иерархические H2–H4 без пропусков уровней;
* [x] осмысленные абзацы, списки и таблицы...
* [x] Markdown-таблицы для небольших простых таблиц...
* [x] изображения через относительные пути, содержательные `alt`-тексты и
  подписи; декоративные изображения помечать как таковые; [ВЫПОЛНЕНО]
    * Изображения нужно обрабатывать поодиночке, чтобы не раздувать контекст.
* [x] сноски, ссылки, цитаты, выделение и код переводятся в стандартный Markdown;
* [x] удалить редакторский мусор...
* [x] расширить неясные сокращения...

После нормализации валидатор должен проверять битые ссылки/пути к медиа,
структуру заголовков, дубли `id`, пустые разделы и маркеры ручной проверки.

**Критерий готовности:** у каждого опубликованного документа корректная
иерархия, рабочие локальные ссылки и нет неотмеченных мест с потерянным
содержимым. [ВЫПОЛНЕНО]

### 4. Метаданные, удобные для ML [DONE]

В начале каждого нормализованного файла хранить YAML front matter.

1. [x] Схема метаданных реализована.
2. [x] `id` стабилен.
3. [x] `tags`, `aliases` и `document_type` контролируются словарём.
4. [x] `content/manifests/issues.json` ведётся.

### 5. Подготовка к индексации [DONE]

1. [x] Собирать из Markdown версионированный JSONL-каталог документов...
2. [x] Делить документы по смысловой структуре...
3. [x] Целевой размер фрагмента — примерно 400–800 токенов...
4. [x] В каждый чанк добавлять `chunk_id`...
5. [x] Исключать из ML-слоя служебные комментарии...

### 6. Контроль качества и выпуск [DONE]

1. [x] Автоматически сверять число документов... [ВЫПОЛНЕНО]
2. [x] Провести выборочную визуальную сверку... [ВЫПОЛНЕНО]
3. [x] Провести семантическую сверку... [ВЫПОЛНЕНО]
4. [x] Выпускать корпус с `CHANGELOG`... [ВЫПОЛНЕНО]

### Версия 1.1.0: Hugging Face публикация [IN PROGRESS]

#### 7. Подготовка для Hugging Face

1. [ ] Создать `scripts/prepare_for_huggingface.py`:
   - Конвертировать `chunks.jsonl` в HF Dataset format
   - Валидировать совместимость с HF Hub
   - Генерировать статистику (размер, количество образцов)
   - Поддержка multiple config (chunks, documents, images)

2. [ ] Создать `scripts/upload_to_huggingface.py`:
   - Аутентификация через `HF_TOKEN` из `.env`
   - Загрузка датасета на HF Hub
   - Управление версиями (теги, ветки)
   - Валидация после загрузки

3. [ ] Создать `hf_dataset_README.md` (Dataset Card):
   - Полное описание датасета
   - Лицензирование (CC0 текст + авторское право на изображения)
   - Примеры использования
   - Статистика (39 документов, 1378 чанков, 132 изображения)
   - Цитирование
   - Биография авторства

4. [ ] Создать `dataset_info.yaml`:
   - Конфигурация датасета для HF
   - Схема признаков для каждого config
   - Метаданные: язык, лицензия, размер

5. [ ] Создать `scripts/test_hf_dataset.py`:
   - Проверка совместимости с `datasets` библиотекой
   - Примеры загрузки разных конфигов
   - Валидация целостности данных

6. [ ] Создать `docs/HUGGINGFACE_SETUP.md`:
   - Пошаговый гайд по публикации на HF
   - Требования и предусловия
   - Полный workflow: prepare → test → upload
   - Управление доступом и приватностью
   - Обновление версий и миграция

7. [ ] Обновить `.env.example`:
   - Добавить `HF_TOKEN` и `HF_REPO_ID`
   - Документировать переменные окружения

8. [ ] Обновить `.gitignore`:
   - Исключить `.env` и `.env.local`
   - Исключить временные файлы при загрузке

**Критерий готовности:** датасет успешно загружен на HF Hub, доступен для загрузки 
через `datasets.load_dataset()`, имеет полную документацию и примеры использования.

## Очерёдность итераций

### Итерация 1: Подготовка корпуса [DONE]

1. Утвердить лицензию, набор выходных форматов и словарь `document_type`/тегов.
2. Добавить манифест и скрипт инвентаризации.
3. Установить и зафиксировать конвертер, затем сделать пробный экспорт 3–5
   репрезентативных файлов: обычной главы, документа с таблицами, шаблона,
   «Врезки» и документа с изображениями.
4. По результатам пробы зафиксировать правила нормализации и автоматизировать
   их.
5. Конвертировать весь корпус, провести вычитку по приоритету, затем построить
   JSONL и чанки.

### Итерация 2: Hugging Face публикация [IN PROGRESS]

1. Создать скрипты для подготовки и загрузки на HF
2. Написать полную документацию (Dataset Card, примеры)
3. Тестировать совместимость с HF ecosystem
4. Загрузить на HF Hub и валидировать доступность

## Что не входит в первый выпуск (v1.0.0)

* изменение авторского содержания, фактов или стиля;
* генерация новых игровых материалов моделью;
* выбор конкретной векторной БД или модели эмбеддингов — корпус должен быть
  независим от них;
* OCR – не нужен, однако требуется составление описаний для тех изображений, что пока не распознаны.

## Что не входит во второй выпуск (v1.1.0)

* интеграция с конкретными облачными ML-платформами (Replicate, Runway и т.п.);
* веб-интерфейс для RAG (выполнится как отдельный проект);
* дообучение моделей (fine-tuning) — только подготовка данных;
* визуальный поиск по изображениям — требует отдельного векторного индекса.

## Что входит в возможные итерации 3+ (v1.2.0+)

* Интеграция с LangChain/LlamaIndex
* Визуальный поиск (CLIP embeddings для 132 изображений)
* Веб-приложение с RAG и UI (FastAPI + React/Vue)
* Облачные интеграции (Google Cloud, AWS)
* Дообучение базовых моделей (DPO/SFT примеры)
